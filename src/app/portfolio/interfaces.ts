export interface Gallery {
   slug: string;
    grid: string;
    images: any[];
    title: string;
 }

 export interface GalleryThumb {
   slug: string;
   title:string;
   thumb: string;

 }

 export interface Commission {
  slug: string;
  content: string;
  images: string;
  title: string;
 }
